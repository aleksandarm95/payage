namespace Payage.Domain.Payments
{
    public class CardMask
    {
        // Value object placeholder representing a masked card number
        // (PAN masking rules), without storing sensitive data
    }
}
